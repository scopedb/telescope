extensions:
  health_check:
  file_storage:
    directory: /var/lib/vendor-otelcol/queue
    create_directory: true

receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

processors:
  memory_limiter:
    check_interval: 1s
    limit_mib: 512
    spike_limit_mib: 128
  batch:
    timeout: 5s
    send_batch_size: 8192

exporters:
  vendordb:
    endpoint: ${env:SCOPEDB_ENDPOINT}
    path: /v1/ingest
    api_key: ${env:SCOPEDB_API_KEY}
    dataset: default
    create_tables_if_not_exist: true
    schema_version: v1
    compression: zstd
    timeout: 10s
    retry_on_failure:
      enabled: true
      initial_interval: 1s
      max_interval: 30s
      max_elapsed_time: 0s
    sending_queue:
      enabled: true
      queue_size: 10000
      num_consumers: 4
      storage: file_storage

service:
  extensions: [health_check, file_storage]
  pipelines:
    logs:
      receivers: [otlp]
      processors: [memory_limiter, batch]
      exporters: [vendordb]
    traces:
      receivers: [otlp]
      processors: [memory_limiter, batch]
      exporters: [vendordb]
    metrics:
      receivers: [otlp]
      processors: [memory_limiter, batch]
      exporters: [vendordb]
