# cmd

This directory stores runnable service entrypoints for the API service.

Current binaries:

- `scopedb-otel-debug-api`: Echo HTTP service for the semantic debug API
- `scopedb-otel-mcp`: stdio MCP server for local developer agents
